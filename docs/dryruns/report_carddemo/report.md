# Relian Data Discovery report — AWS CardDemo (public sample, dry run)
> **This document is derived and unsigned.** The signed artifact is `report.json`; this file is a rendering of it. If the two ever disagree, `report.json` is authoritative and this rendering is the defect. Verify with `python3 tools/verify_report.py --report-dir <directory> --pin-fingerprint <release key fingerprint>`.

**Report id:** `2fff4d2377c2cc199d6271bd1c21f885`

## 1. Identification

| Field | Value |
| --- | --- |
| Customer | AWS CardDemo (public sample, dry run) |
| Engagement | Data Discovery — WP-2.3 dry run |
| Contract vehicle | internal dry run, not a delivered engagement |
| Relian version | 0.1.0 (from the source checkout's pyproject.toml) |
| Scanned tree | aws-mainframe-modernization-carddemo @ 59cc6c2 |
| Instance key fingerprint | `98616f9c7543d293` |

This installation's Ed25519 instance key was generated by Relian on first use, at ~/.relian/instance-ed25519.pem with mode 0600, and is recorded here rather than left implicit. If the key cannot be created or loaded, Relian emits no report at all: an unsigned artifact that looks signed is worse than no artifact.

The instance signature proves integrity and provenance-of-tool: this report was produced by this Relian installation over inputs with the digests recorded in the manifest, and has not changed since. It does NOT prove identity of signer. The instance key is generated on this machine and never leaves it; Visionblox has never held it and cannot vouch for it. A report carrying only this layer is VALID AND UNATTESTED. Only the Visionblox countersignature attests to the report, and it is a separate, detached file added by the operator on delivery.

Customer source never leaves this machine. The report and its manifest are produced here; the manifest records SHA-256 digests of the inputs, never their content. The only value that crosses the perimeter to obtain a Visionblox countersignature is the 64-character manifest hash, together with the report id and the instance key fingerprint — all three of which are hexadecimal digests, not text drawn from your code.

**Limitation, stated up front:** Verified byte-for-byte against GnuCOBOL 3.1.2.0 on RELIAN-DISCOVERY-BENCH v0.1 (relian-discovery-bench-v0.1), 186 of 186 comparisons at tolerance zero. Equivalence with IBM Enterprise COBOL is UNMEASURED: SYNCHRONIZED slack in particular moves with the compiler's binary-size setting and its SYNCHRONIZED handling, so an IBM layout may differ from the one below. Grade PLAUSIBLE, not VERIFIED, for that reason.

## 2. Scope

**Grade:** VERIFIED · **Provenance:** every row below was counted by this run over `aws-mainframe-modernization-carddemo @ 59cc6c2`.

| Measure | Value |
| --- | --- |
| copy directive sites | 346 |
| copy replacing sites | 40 |
| cycles | 0 |
| distinct names referenced | 67 |
| edges | 306 |
| files with at least one copy | 40 |
| members present but unreferenced | 3 |
| resolvable | 59 |
| source files scanned | 106 |
| unresolvable | 8 |

Copybook resolution reads COPY directives, not storage, so it is independent of the COBOL dialect and no compiler-dialect limitation applies to the counts above. Record layouts are a different claim and carry their own limitation.

## 3. Missing copybooks

Every COPY target below was referenced by the code you supplied and could not be found under any directory this run searched. Each row carries the full referrer list and the number of directories searched, because "missing" without "we looked here" is an accusation rather than a finding. A copybook that resolves at your site and not at ours is the normal condition of real mainframe code; it is not a defect in your submission, and it is the part of your migration that has to be sourced before a record layout can be computed for the members that reference it.

| Missing copybook | Referenced by (files) | COPY sites | Directories searched |
| --- | --- | --- | --- |
| `DFHAID` | 21 | 21 | 51 |
| `DFHBMSCA` | 21 | 21 | 51 |
| `CMQGMOV` | 3 | 3 | 51 |
| `CMQMDV` | 3 | 4 | 51 |
| `CMQODV` | 3 | 4 | 51 |
| `CMQPMOV` | 3 | 3 | 51 |
| `CMQTML` | 3 | 3 | 51 |
| `CMQV` | 3 | 3 | 51 |

This report does not guess where a missing member comes from. The referrer list and the search-path count are measured; the origin of a member this run never saw is not, and an inference printed beside measurements would read as one.

## 4. Record layouts

Not included in this report. Resolver-only by decision, not by failure. WP-2.2 section 10 established that no layout claim is made about any CardDemo record: the layout engine is verified against GnuCOBOL 3.1.2.0 and CardDemo is IBM-dialect source, so a layout here would be graded against nothing. Copybook resolution is dialect-independent and is unaffected.

## 5. Copybook fan-in

| Copybook | Referenced by (distinct files) |
| --- | --- |
| `COCOM01Y` | 21 |
| `COTTL01Y` | 21 |
| `CSDAT01Y` | 21 |
| `CSMSG01Y` | 21 |
| `DFHAID` | 21 |
| `DFHBMSCA` | 21 |
| `CSUSR01Y` | 14 |
| `CVACT01Y` | 14 |
| `CVACT03Y` | 14 |
| `CVTRA05Y` | 11 |
| `CVACT02Y` | 10 |
| `CVCUS01Y` | 10 |
| `CIPAUDTY` | 8 |
| `CIPAUSMY` | 7 |
| `CSMSG02Y` | 7 |
| `CSSTRPFY` | 7 |
| `CVCRD01Y` | 7 |
| `CMQGMOV` | 3 |
| `CMQMDV` | 3 |
| `CMQODV` | 3 |
| `CMQPMOV` | 3 |
| `CMQTML` | 3 |
| `CMQV` | 3 |
| `IMSFUNCS` | 3 |
| `PAUTBPCB` | 3 |
| `CSSETATY` | 2 |
| `CSUTLDWY` | 2 |
| `CVEXPORT` | 2 |
| `CVTRA01Y` | 2 |
| `CVTRA06Y` | 2 |
| `CCPAUERY` | 1 |
| `CCPAURLY` | 1 |
| `CCPAURQY` | 1 |
| `COACTUP` | 1 |
| `COACTVW` | 1 |
| `COADM01` | 1 |
| `COADM02Y` | 1 |
| `COBIL00` | 1 |
| `COCRDLI` | 1 |
| `COCRDSL` | 1 |
| `COCRDUP` | 1 |
| `CODATECN` | 1 |
| `COMEN01` | 1 |
| `COMEN02Y` | 1 |
| `COPAU00` | 1 |
| `COPAU01` | 1 |
| `CORPT00` | 1 |
| `COSGN00` | 1 |
| `COSTM01` | 1 |
| `COTRN00` | 1 |
| `COTRN01` | 1 |
| `COTRN02` | 1 |
| `COTRTLI` | 1 |
| `COTRTUP` | 1 |
| `COUSR00` | 1 |
| `COUSR01` | 1 |
| `COUSR02` | 1 |
| `COUSR03` | 1 |
| `CSLKPCDY` | 1 |
| `CSUTLDPY` | 1 |
| `CUSTREC` | 1 |
| `CVTRA02Y` | 1 |
| `CVTRA03Y` | 1 |
| `CVTRA04Y` | 1 |
| `CVTRA07Y` | 1 |
| `PADFLPCB` | 1 |
| `PASFLPCB` | 1 |

Fan-in is the number of DISTINCT files that reference a member. A file that COPYs the same member twice is two directive sites and one edge; conflating them would make two different counts look like the same measurement.

## 6. Limitations and grades

* Verified byte-for-byte against GnuCOBOL 3.1.2.0 on RELIAN-DISCOVERY-BENCH v0.1 (relian-discovery-bench-v0.1), 186 of 186 comparisons at tolerance zero. Equivalence with IBM Enterprise COBOL is UNMEASURED: SYNCHRONIZED slack in particular moves with the compiler's binary-size setting and its SYNCHRONIZED handling, so an IBM layout may differ from the one below. Grade PLAUSIBLE, not VERIFIED, for that reason.

**What this report does not claim**

* No risk score of any kind is computed. Relian has no scoring model, and a number produced without one would be a number without a measurement behind it.
* No file inventory, data lineage, or target-schema DDL. Those are later work and are absent here rather than approximated.
* No claim about any language pair other than COBOL. There is no benchmark and no transpiler for one, so there is nothing to claim.
* No claim of equivalence with IBM Enterprise COBOL. Everything the layout engine is verified against is GnuCOBOL 3.1.2.0.

## 7. How to verify this report

Run: python3 tools/verify_report.py --report-dir <this directory> --pin-fingerprint <the Visionblox release key fingerprint from the Technical Delivery Sheet>. The verifier is standalone: standard library plus `cryptography`, no network, no credentials, and no import from the Relian source tree. It prints its own SHA-256 so you can confirm it is the tool that was shipped to you. Exit 0 means all four layers passed. Exit 3 means the report is VALID AND UNATTESTED — the instance layer passed and no Visionblox countersignature is present. Any other non-zero exit means a named layer failed.

| Layer | What it checks |
| --- | --- |
| FILES | every path recorded in the manifest exists and hashes to its recorded SHA-256 |
| MANIFEST | the manifest hash, recomputed from the manifest minus its signature block, equals the value the signature was taken over |
| INSTANCE | the Ed25519 instance signature verifies over that manifest hash, under the fingerprint the manifest records |
| COUNTERSIGNATURE | the detached Visionblox countersignature verifies over the same manifest hash, under the release-key fingerprint YOU pin on the command line. The pin is required: an unpinned check would accept a report re-signed under any key at all. |

The instance signature proves integrity and provenance-of-tool: this report was produced by this Relian installation over inputs with the digests recorded in the manifest, and has not changed since. It does NOT prove identity of signer. The instance key is generated on this machine and never leaves it; Visionblox has never held it and cannot vouch for it. A report carrying only this layer is VALID AND UNATTESTED. Only the Visionblox countersignature attests to the report, and it is a separate, detached file added by the operator on delivery.
